import { ClusterDetail } from '@/services/cluster';

export type ClusterProps = {
  cluster: string;
};
const ClusterDashboardView: React.FC<ClusterProps> = (props) => {
  return <></>;
};
export default ClusterDashboardView;
